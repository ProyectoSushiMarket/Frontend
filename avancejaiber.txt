Poner el diseño como si fuera una factura, que primero sea el cliente, segundo sea el nombre del producto, el tercero la fecha y el cuarto la cantidad,

El producto debe de tener Unidad de medida, cantidad, características, responsable (cliente), la fecha no se puede modificar que sea la actual del día que se pedirá el producto,
 exportar todos los pedidos en PDF, quitar la foto de los pedidos, agregar botón de clientes y que el administrador pueda registrar los clientes


// hacer un pedido (cliente)
 <!-- Modal para agregar producto -->
    <div id="add-product-modal" class="add-product-modal">
        <div class="add-product-modal-content">
            <div class="add-product-modal-header">Agregar Producto</div>
            <div class="add-product-modal-body">
                <form id="product-form">
                    <label for="product-name">Nombre del producto:</label>
                    <input type="text" id="product-name" name="product-name" placeholder="Ingresa el nombre del producto">
            
                    <label for="product-cantidad">Cantidad:</label>
                    <input type="text" id="product-cantidad" name="product-cantidad" placeholder="Ingresa la cantidad">

                    <label for="product-UnidadDeMedida">Unidad de medida:</label>
                    <input type="text" id="product-UnidadDeMedida" name="product-UnidadDeMedida" placeholder="Ingresa la unidad de medida">
                        
                    <label for="product-caracteristicas">Caracteristicas:</label>
                    <input type="text" id="product-caracteristicas" name="product-caracteristicas" placeholder="Caracteristicas">
            
                    <label for="product-image">Imagen del producto:</label>
                    <input type="text" id="product-image" name="product-image" placeholder="Ingresa la URL de la imagen" style="width: 100%;">
                </form>
            </div>
            <div class="add-product-modal-actions">
                <button class="save-product-button" onclick="guardarCambios()">Guardar</button>
                <button class="cancel-product-button" onclick="cerrarModal()">Cancelar</button>
            </div>
        </div>
    </div>

        
    <script>
            // Función para abrir el modal
            function abrirModal() {
                document.getElementById("add-product-modal").style.display = "flex";
            }

            // Función para cerrar el modal
            function cerrarModal() {
                document.getElementById("add-product-modal").style.display = "none";
            }

            // Función para guardar el producto
            function guardarCambios() {
                // Capturar los valores directamente desde el formulario
                const nombre = document.getElementById("product-name").value;
                const cantidad = document.getElementById("product-cantidad").value;
                const unidadmedida = document.getElementById("product-UnidadDeMedida").value;
                const caracteristicas = document.getElementById("product-caracteristicas").value;
                const imagen = document.getElementById("product-image").value;

                // Crear el objeto con los datos para enviar
                const datos = {
                    nombre: nombre,
                    cantidad: cantidad,
                    unidadmedida: unidadmedida,
                    caracteristicas: caracteristicas,
                    imagen: imagen
                };

                // Enviar los datos al backend
                fetch('http://localhost:4100/producto', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                    },
                    body: JSON.stringify(datos),
                })
                    .then((response) => {
                        if (!response.ok) {
                            throw new Error('Error al guardar los datos');
                        }
                        return response.json();
                    })
                    .then((data) => {
                        console.log('Producto guardado con éxito:', data)
                        alert("Producto Agregado")
                        cerrarModal();
                        location.reload(); // Recarga la página automáticamente
                    })
                    .catch((error) => {
                        console.error('Error al guardar el producto:', error);
                        alert('No se pudo guardar el producto');
                    });
            }

    </script>